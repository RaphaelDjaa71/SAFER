{% extends 'base.html.twig' %}

{% block content %}
    <h1>Inscription</h1>
    <p>Pour créer un nouveau compte</p>

    {% if notification %}
        <div class="alert alert-info">{{ notification }}</div>
        <hr>
    {% endif %}
    {{ form(form) }}
{% endblock %}
