{% extends 'base.html.twig' %}

{% block title %}Nos biens - SAFER{% endblock %}

{% block content %}
    <h1>Nos biens immobilers</h1>

    <div class="row">
        {% for Biens in biens %}
            <div class="col-md-4">
                <div class="Biens-item text-center">
                    <img src="{{ asset('/uploads/'~ Biens.illustration) }}" alt="{{ Biens.title }}" class="img-fluid">
                    <h5>{{ Biens.title }}</h5>
                    <span class="Biens.description">{{ Biens.description }}</span>
                    <br><span class="Biens.price">{{ (Biens.price / 100)|number_format(2) }}€</span><br>
                </div>
            </div>
        {% endfor %}
    </div>
{% endblock %}